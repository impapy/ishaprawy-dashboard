import React from "react"
import Layout from "../components/UI/Layout"

const DesignedProducts = () => {
  return <Layout title="Designed products">DesignedProducts</Layout>
}

export default DesignedProducts
